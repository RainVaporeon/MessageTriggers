package com.spiritlight.messagetriggers.core;

public class Note {


    /**
     * TODO: Find out why {@link TriggerFunctionManager} fires twice,
     * or why {@link MessageListener} fires twice, and then unregisters
     * itself
     */
    public static final Note note = new Note();
}
